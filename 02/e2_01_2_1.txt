A, B:Java における == は数字以外においてはまったく同じObjectであるときのみtrueをかえすため
C: コード内で obj4 = obj2 を記述しているためobj4とobj2は全く同じObjectとして機能するため
D: ObjectとしてもちがいIDと名前も違うため。
E: .equalsをオーバーライドしたことによりObjectとしては違うがIDと名前が同じものも同一として扱うようにしたため。
F: .equalsの本来の機能を損なわないように実装したため。